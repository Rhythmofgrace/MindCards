# Mindcards

A minimal infinite-canvas flashcard prototype. Drag to pan, scroll to zoom, and click questions to reveal their answers.

Open `index.html` in a browser. For a local server, run `python -m http.server 8000` and visit `http://localhost:8000`.
